services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - db-data:/app/db
    environment:
      - DATABASE_URL=file:/app/db/custom.db
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  db-data:
